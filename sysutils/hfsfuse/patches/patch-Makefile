$OpenBSD$

Index: Makefile
--- Makefile.orig
+++ Makefile
@@ -40,6 +40,9 @@ else ifeq ($(OS), DragonFly)
 	APP_FLAGS += -I/usr/local/include
 	APP_LIB += -L/usr/local/lib
 	FUSE_FLAGS += -I/usr/local/include
+else ifeq ($(OS), OpenBSD)
+	APP_FLAGS += -I/usr/local/include
+	APP_LIB += -L/usr/local/lib
 endif
 
 PREFIX ?= /usr/local
